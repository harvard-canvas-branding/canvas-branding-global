# canvas-branding
Top-level Canvas theme files.

## Build Process

Creating a release in this repository automatically triggers a GitHub Actions workflow that builds the project and generates minified versions of the CSS and JS files.

The generated minified CSS and JS files are uploaded to the GitHub release and can be downloaded from there.
